<%= form_for(sample_action) do |f| %>
  <% if sample_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample_action.errors.count, "error") %> prohibited this sample_action from being saved:</h2>

      <ul>
      <% sample_action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
